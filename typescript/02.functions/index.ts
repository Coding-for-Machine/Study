

function add(a: number, b: number): number {
    return a+b;
}

console.log(add(5, 15));

const add_string = (first_name: string, last_name: string): string => {
    return first_name + " " +last_name;
}

console.log(add_string("Asadbek", "Nurmamatov"));